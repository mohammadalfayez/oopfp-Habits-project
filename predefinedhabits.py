import datetime

predifined = [
    [
        'Exercise',
        'weekly',
         datetime.date.today(),
         datetime.date.today() + datetime.timedelta(days=7),
    ]
,
    [
        'Study',
        'daily',
         
        datetime.date.today(),
        datetime.date.today() + datetime.timedelta(days=1),
    ]
,
    [
        'Take vitamins',
        'daily',
        datetime.date.today(),
        datetime.date.today() + datetime.timedelta(days=1),
    ]
,
    [
        'Drink water',
        'daily',
        datetime.date.today(),
        datetime.date.today() + datetime.timedelta(days=1),
    ]
,
    [
        'Take a walk',
        'daily',
         
        datetime.date.today(),
         datetime.date.today() + datetime.timedelta(days=1),
    ]
]
